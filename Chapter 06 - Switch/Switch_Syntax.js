// Switch statement in javaScript
// The switch statement evaluates an expression once and then compares against multiple values
// When the match is found, the code for that case runs.
// Switch always uses === (thriple equals) 

/*Switch (expresssion) {
   case value1:
        //code block
        break;  // important: stops fall-through
        case value2:
        //code block
        break;
        default:
            //runs if no case matches (optional)

} 
*/
