let fruit =[];
let browsers = ["chrome", "Mozilla", "Edge"];
console.log(browsers[0]);
console.log(browsers[-2]);
console.log(browsers.length);
console.log(fruit.length);

//For negative index, use the at (at is a function)
console.log(browsers.at(-1));