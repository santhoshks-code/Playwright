// Array literal

let browsers = ["Chrome", "Mozialla", "Edge"];

// Array constuctor
let scores = new Array(3); // creates empty
let scores2 = new Array(1,2,3);

let numbers = new Array(100, 200, 300, 400);

let test = Array.of(10, 20, 30, 40, 50);
console.log(test.length);

// Array from()
let chars = Array.from(5);
console.log(chars);