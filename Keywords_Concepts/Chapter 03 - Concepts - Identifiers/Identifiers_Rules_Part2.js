var firstName = "John";
var lastName = "Doe";
console.log(firstName + " " + lastName);
// Camel case naming convention
// The first letter of the first word is lowercase, and the first letter of each subsequent word is uppercase
// Long variable names can be created using camel case, which improves readability
// underscore naming convention is allowed, but it is not recommended for variable names in JavaScript. It is better to use camel case for variable names to improve readability and maintainability of the code.

var middleName = "Michael";
console.log(middleName);